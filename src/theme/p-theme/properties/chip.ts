import {
  ChipDesignTokens,
  ChipTokenSections,
} from '@primeuix/themes/types/chip';

export const image: ChipTokenSections.Image = {
  width: '20px',
  height: '20px',
};

export const root: ChipTokenSections.Root = {
  paddingX: '20px',
};

export default {
  image,
  root,
} satisfies ChipDesignTokens;
