import {
  TimelineDesignTokens,
  TimelineTokenSections,
} from '@primeuix/themes/types/timeline';

export const eventMarker: TimelineTokenSections.EventMarker = {
  content: {
    background: 'black',
  },
};

export default {
  eventMarker,
} satisfies TimelineDesignTokens;
